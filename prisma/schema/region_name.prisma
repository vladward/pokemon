model region_name {
  region_id Int
  language  String @db.VarChar(10)
  name      String @db.VarChar(200)
  region    region @relation(fields: [region_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "region_name_ibfk_1")

  @@id([region_id, language])
}
