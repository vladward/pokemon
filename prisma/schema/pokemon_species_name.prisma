model pokemon_species_name {
  species_id Int
  language   String @db.VarChar(10)
  name       String @db.VarChar(200)

  species    pokemon_species @relation(fields: [species_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@id([species_id, language])
}
