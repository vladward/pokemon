import type {FC} from 'react';
import {Header} from "@/widgets/Header";
import './global.scss'

export const App: FC = () => {
    return (
        <div>
            <Header />
        </div>
    );
};